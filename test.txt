# Test for the interior point algorithm
G = np.array([[1,-1],[-1,2]])
c = np.array([-2, -6])
const = 0
A = np.array([[-1/2,-1/2],[1,-2],[1,0],[0,1]])
b = np.array([-1,-2,0,0])

ip = IntPoint(G, A, b, c, max_iteration=100, verbose=False)
ip.solve()
ip.print_solution()